using System.Threading;
using UnityEngine;

public class GrapleHookScript : MonoBehaviour
{
    

    // Update is called once per frame
    void Update()
    {
        
    }

    public void OnGrapleHold(ContextCallback context)
    {
        if()
    }
}
